springboot注解:
    @Controller: 被注解的类下所有方法return到前端页面
    @ResponseBody: 注解方法return数据
    @RestController: 相当于@Controller+@ResponseBody
    @GetMapping: 以get方式请求接口
    @RequestMapping:
    @DeleteMapping: 以delete方式请求接口